//phon
import 'package:flutter/material.dart';

class BLoanStatus extends StatelessWidget {
  const BLoanStatus({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("BLoanStatus"),
      ),
      body: const Center(
        child: Text("Create Screen"),
      ),
    );
  }
}

//phon
import 'package:flutter/material.dart';

class BPaymentScreen extends StatelessWidget {
  const BPaymentScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("BPaymentScreen"),
      ),
      body: const Center(
        child: Text("Create Screen"),
      ),
    );
  }
}

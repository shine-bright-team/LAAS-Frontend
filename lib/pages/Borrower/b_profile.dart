//fxh
import 'package:flutter/material.dart';

class BProfileScreen extends StatelessWidget {
  const BProfileScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("LProfileScreen"),
      ),
      body: const Center(
        child: Text("Create Screen"),
      ),
    );
  }
}

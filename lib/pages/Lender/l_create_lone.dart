//phon
import 'package:flutter/material.dart';

class LCreateLoan extends StatelessWidget {
  const LCreateLoan({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("LCreateLoan"),
      ),
      body: const Center(
        child: Text("Create Screen"),
      ),
    );
  }
}
